#include <stdio.h>

/**
 * Generate a checkerboard.ppm file with the following specification:
 * - Binary PPM format (P6)
 * - Image dimensions: 960 × 540 pixels
 * - Checkerboard made of 60 × 60 pixel squares
 * - Alternating pure red (255, 0, 0) and pure black (0, 0, 0)
 * - Top-left square must be red
 * - No external libraries allowed
 */
int main() {
    // Your code here

    return 0;
}